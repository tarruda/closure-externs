/** @constructor */
jQuery.terminal.options = function() {};

jQuery.terminal.options.prototype.greetings;

jQuery.terminal.options.prototype.tabcompletion;

jQuery.terminal.options.prototype.name;

jQuery.terminal.options.prototype.height;

jQuery.terminal.options.prototype.prompt;

jQuery.terminal.options.prototype.enabled;

/**
 * @param {=(object)} interpreter
 * @param {=(string|jQuery.terminal.options)} termOpts
 * @return {object}
 */
jQuery.prototype.terminal = function(interpreter, termOpts) {};
